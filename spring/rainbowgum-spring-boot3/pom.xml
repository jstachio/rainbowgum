<project xmlns="http://maven.apache.org/POM/4.0.0"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>io.jstach.rainbowgum</groupId>
    <artifactId>rainbowgum-spring-parent</artifactId>
    <version>0.10.0-SNAPSHOT</version>
  </parent>
  <artifactId>rainbowgum-spring-boot3</artifactId>
  <name>rainbowgum-spring-boot3</name>
  <properties>
    <!--
      Deliberately self-contained (not inherited from rainbowgum-spring-parent):
      rainbowgum-spring-boot3 and rainbowgum-spring-boot4 each pin their own Spring Boot
      major version and must never share a dependencyManagement import.
    -->
    <spring-boot.version>3.3.3</spring-boot.version>
    <doc.resources>../../</doc.resources>
    <parent.root>${basedir}/../..</parent.root>
  </properties>
  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>${spring-boot.version}</version>
        <scope>import</scope>
        <type>pom</type>
      </dependency>
    </dependencies>
  </dependencyManagement>
  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <!--
            The module name is deliberately suffixed with the Spring Boot major version
            (see module-info.java) so this specific, well-understood lint warning is
            suppressed rather than uglifying the name - it only matters for jars with no
            explicit module-info.java (automatic module name inference), which we never
            rely on.
          -->
          <compilerArgs combine.children="append">
            <arg>-Xlint:-module</arg>
          </compilerArgs>
        </configuration>
      </plugin>
    </plugins>
  </build>
  <dependencies>
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>rainbowgum-core</artifactId>
    </dependency>
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>rainbowgum-pattern</artifactId>
    </dependency>
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>rainbowgum-spring-spi</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot</artifactId>
    </dependency>
    <!--
      rainbowgum-tomcat routes Tomcat's own internal logging (org.apache.juli.logging.Log,
      discovered via ServiceLoader) straight through RainbowGum's LogRouter instead of
      java.util.logging. It is a small jar whose own dependency on tomcat-juli is provided
      and optional, so pulling it in here costs almost nothing for users on a different
      embedded web server (Jetty, Undertow) - the service provider simply never gets used.
    -->
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>rainbowgum-tomcat</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>io.jstach.pistachio</groupId>
      <artifactId>pistachio-svc</artifactId>
    </dependency>
    <dependency>
      <groupId>io.jstach.pistachio</groupId>
      <artifactId>pistachio-svc-apt</artifactId>
    </dependency>
  </dependencies>
</project>
