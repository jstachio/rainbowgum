<project xmlns="http://maven.apache.org/POM/4.0.0"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>io.jstach.rainbowgum</groupId>
    <artifactId>rainbowgum-benchmark-parent</artifactId>
    <version>0.10.0-SNAPSHOT</version>
  </parent>
  <artifactId>rainbowgum-benchmark-micro-parent</artifactId>
  <packaging>pom</packaging>
  <properties>
    <parent.root>${basedir}/../..</parent.root>
  </properties>
  <!--
    A from-scratch, no-Spring microbenchmark of Logback/Log4j2/RainbowGum through the plain
    SLF4J facade (not log4j2's own native API), console output, common cross-compatible
    pattern. Deliberately separate from the older rainbowgum-benchmark-{logback,log4j,
    rainbowgum,share} modules in the parent directory - this supersedes them in ambition
    (multiple message shapes, level-check cost, SLF4J 2.x fluent EventBuilder API, single/
    platform/virtual threading) but doesn't replace or edit them.
  -->
  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>${project.groupId}</groupId>
        <artifactId>rainbowgum-benchmark-micro-share</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>ch.qos.logback</groupId>
        <artifactId>logback-classic</artifactId>
        <version>1.5.34</version>
      </dependency>
      <dependency>
        <groupId>org.apache.logging.log4j</groupId>
        <artifactId>log4j-slf4j2-impl</artifactId>
        <version>2.25.4</version>
      </dependency>
      <dependency>
        <groupId>org.apache.logging.log4j</groupId>
        <artifactId>log4j-core</artifactId>
        <version>2.25.4</version>
      </dependency>
    </dependencies>
  </dependencyManagement>
  <modules>
    <module>rainbowgum-benchmark-micro-share</module>
    <module>rainbowgum-benchmark-micro-logback</module>
    <module>rainbowgum-benchmark-micro-log4j2</module>
    <module>rainbowgum-benchmark-micro-rainbowgum</module>
  </modules>
</project>
