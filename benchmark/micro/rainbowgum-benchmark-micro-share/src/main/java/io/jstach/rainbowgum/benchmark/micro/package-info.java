/**
 * Shared driver for the from-scratch (no Spring) SLF4J logging microbenchmark: scenarios,
 * threading modes, and result reporting used by each framework's own module.
 */
package io.jstach.rainbowgum.benchmark.micro;
