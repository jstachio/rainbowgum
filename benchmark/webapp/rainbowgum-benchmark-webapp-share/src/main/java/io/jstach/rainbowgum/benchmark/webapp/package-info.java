/**
 * Shared REST controller and MDC filter used by every logging-backend benchmark app, so
 * each app under test does exactly the same work.
 */
@org.eclipse.jdt.annotation.NonNullByDefault
package io.jstach.rainbowgum.benchmark.webapp;
