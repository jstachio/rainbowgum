<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>io.jstach.rainbowgum</groupId>
    <artifactId>rainbowgum-test-parent</artifactId>
    <version>0.12.0-SNAPSHOT</version>
  </parent>
  <!--
    Tests the SLF4J-bridge path specifically (org.jboss.logging.Slf4jLocationAwareLogger,
    routed through rainbowgum-slf4j), as a deliberate contrast to the native
    rainbowgum-jboss-logging module's own tests (RainbowGumJBossLoggerProviderTest),
    which need none of the workaround below - see that module's tests/module-info for why
    it is now the recommended path. This module stays as regression coverage for anyone
    not using the native module.
  -->
  <artifactId>rainbowgum-test-jboss-logging-bridge</artifactId>
  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <!--
            JBoss Logging does not auto-select SLF4J just because slf4j-api is on the
            classpath (confirmed by decompiling LoggerProviders.findProvider(): the
            fallback chain after this system property is jboss-logmanager, log4j2,
            logback, then plain JDK, never bare SLF4J). Without this it silently picks
            java.util.logging (org.jboss.logging.JDKLogger), never reaching RainbowGum
            at all. Must be a JVM system property, not set programmatically in the
            test, since LoggerProviders.PROVIDER is a static final field resolved once
            at class load time.
          -->
          <systemPropertyVariables>
            <org.jboss.logging.provider>slf4j</org.jboss.logging.provider>
          </systemPropertyVariables>
        </configuration>
      </plugin>
    </plugins>
  </build>
  <dependencies>
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>rainbowgum-slf4j</artifactId>
    </dependency>
    <dependency>
      <groupId>org.jboss.logging</groupId>
      <artifactId>jboss-logging</artifactId>
    </dependency>
  </dependencies>
</project>
