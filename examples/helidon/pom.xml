<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>rainbowgum.helidon.example</groupId>
  <artifactId>rainbowgum-helidon-example</artifactId>
  <version>0.1</version>
  <packaging>jar</packaging>

  <properties>
    <maven.compiler.release>21</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <helidon.version>4.5.5</helidon.version>
    <exec.mainClass>rainbowgum.helidon.example.Main</exec.mainClass>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>io.helidon</groupId>
        <artifactId>helidon-bom</artifactId>
        <version>${helidon.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>io.helidon.webserver</groupId>
      <artifactId>helidon-webserver</artifactId>
    </dependency>
    <dependency>
      <groupId>io.helidon.logging</groupId>
      <artifactId>helidon-logging-common</artifactId>
    </dependency>

    <!--
      Rainbow Gum: rainbowgum-jdk registers a java.lang.System.LoggerFinder
      (SystemLoggingFactory) that - by default (InitOption.CHECK) - eagerly calls
      RainbowGum.of() itself the first time anything calls System.getLogger(...), which
      the JDK's own internals do incidentally from all sorts of unrelated static init
      (see rainbowgum-systemlogger's own javadoc). rainbowgum-jul is the
      Handler-based JUL bridge (see README.md); rainbowgum-jdk already pulls it in at
      runtime scope, but it is also listed explicitly here since it's what actually
      installs on JUL's root logger once something (rainbowgum-jdk's own eager
      bootstrap, in this example) has triggered a real Rainbow Gum into existence.
    -->
    <dependency>
      <groupId>io.jstach.rainbowgum</groupId>
      <artifactId>rainbowgum-jdk</artifactId>
      <version>1.0.0-SNAPSHOT</version>
    </dependency>
    <dependency>
      <groupId>io.jstach.rainbowgum</groupId>
      <artifactId>rainbowgum-jul</artifactId>
      <version>1.0.0-SNAPSHOT</version>
    </dependency>

    <!--
      Baseline: Helidon's own default JulProvider, JUL config via logging.properties,
      no Rainbow Gum. Swap this dependency for rainbowgum-jul (or rainbowgum) to try
      putting Rainbow Gum underneath - see README.md.
    -->
  </dependencies>

  <build>
    <finalName>rainbowgum-helidon-example</finalName>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
        <configuration>
          <archive>
            <manifest>
              <mainClass>${exec.mainClass}</mainClass>
              <addClasspath>true</addClasspath>
              <classpathPrefix>libs/</classpathPrefix>
            </manifest>
          </archive>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <executions>
          <execution>
            <id>copy-deps</id>
            <phase>package</phase>
            <goals>
              <goal>copy-dependencies</goal>
            </goals>
            <configuration>
              <outputDirectory>${project.build.directory}/libs</outputDirectory>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

</project>
